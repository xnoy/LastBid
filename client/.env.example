# Client environment. Everything here is PUBLIC — it ends up in the bundle.
# Never put an API key, database URL or secret in this file.

# Leave blank in development: vite.config.ts proxies /api and /socket.io to
# http://localhost:4000. Set both in production to your deployed API origin.
VITE_API_URL=
VITE_SOCKET_URL=
